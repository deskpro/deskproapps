{
    "plugins": ["alerts", "anchors", "copy-code-button", "codeblock-label", "custom-favicon", "scripts"],
    "pluginsConfig": {
        "favicon": "favicon.png",
        "scripts": {
            "files": [
                "./scripts/site.js"
            ]
        }
    }
}